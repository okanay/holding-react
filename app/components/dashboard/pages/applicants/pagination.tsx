import React from "react";
import { ChevronLeft, ChevronRight } from "lucide-react";

interface Props {}

export const Pagination: React.FC<Props> = ({}) => {
  const handlePrevious = () => {};

  const handleNext = () => {};

  return <div className="mt-4 text-sm">Pagination (coming soon)</div>;
};
